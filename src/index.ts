// This TypeScript file contains multiple formatting and linting issues

const myFunction = (_name: string, _age: number) => {};

console.log(myFunction("Alice", 25)); // Incorrect spacing, missing semicolon, inconsistent quotes
